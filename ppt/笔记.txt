一、作业
    作业代码(所有) + 运行截图 + 必要的文字&文档说明邮件发压缩包发1941046624@qq.com并抄送834822435@qq.com
    20250921:
        -1. 基于 17flowers.zip 完成深度学习的CV模型训练、推理、部署的相关代码编写
            -a. 训练代码：基于PyTorch的原生代码写训练逻辑
            -b. 推理代码：最终提供一个方法，方法输入图像路径、输出预测的类别信息(类别名称+预测概率)
            -c. 部署: 将推理的方法改成http的接口
            讲解时间: 20250923 + 0925 + 1023
    20250927:
        -1. 在20250921的作业一的基础上，增加一下内容：
            -a. 基于面向对象的思路，把训练代码改一下结构
            -b. 自行熟悉pytorch中的API：
                from torch.utils.data import Dataset, DataLoader
                    Dataset: 负责单条样本的加载解析
                    DataLoader: 多条样本的批次组合(将多个样本合并成一个批次)
            -c. 基于OpenCV增加数据增强的逻辑
                PS: 在每个批次中针对每个样本进行随机增强处理
            -d. debug了解torchvision.models.vgg16_bn内部的代码结构以及相关参数，将17flowers的训练模型直接替换成vgg16_bn，并且参数迁移，最终训练
                PS: 考虑一下其它算法，eg: resnet、mobilenet....
            -e. PS: 将VGG/ResNet/.....的所有层的feature map形成图像均输出，输出到独立的文件夹中：一层一张图片、支持更改输入图像、输出文件夹、输出文件名称格式；
                PS: debug看看forward的执行过程，看看理解一下代码，看看这个代码能不能帮助你更好的进行输出的操作
                    --- nn.Modul._call_impl -- 可能可以进行feature map输出的地方 -- 如何实现这部分
            讲解时间：1014 + 1016 + 1021
        -2. 自行重构一下轮子：各个经典网络的结构
            PS: 基于结构进行重构
            讲解时间：1028
    20251012:
        -1. 将torchvision.models里面的所有模型的参数量、浮点运算量、运行的耗时自己弄一个excel表格
            NOTE: 基于 from thop import profile 自行完善
            讲解时间：1030
    20251019:
        -1. 实现IoU的计算代码
            -a. 给定两个边框的坐标，输出对应的IoU....值
                eg:
                    边框1: 150 200 250 300
                    边框2: 180 230 250 400
                    NOTE: 边框坐标分为两种形式:
                        cx cy w h: 给定的是边框中心点坐标 + 边框的宽度、高度
                        x1 y1 x2 y2: 给定的是边框左上角、右下角的坐标
            -b. 给定的是一个矩阵X: [N,4]，表示N个边框，每个边框对应一个的4维坐标值，计算所有边框与边框之间的IoU，也就是最终结果应该是一个[N,N]的矩阵
            讲解时间: 1030
        -2. 理解&运行一下 models.detection.fasterrcnn_resnet50_fpn & models.detection.ssd300_vgg16 这个目标检测模型
            NOTE:
                a. 构造出来该模型对应的输入，获得得到输出 --> 训练和推理
                b. 列出当前代码的实现和PPT上所讲的Faster R-CNN、SSD有什么差异？
            讲解时间: 20251104
    20251026:
        -1. 用labelimg或者labelme对17种花的数据进行目标检测框的标注，并实现一个voc格式转yolo格式的代码
        -2. 手写数字识别  目标检测数据集生成
            NOTE: 在一张随机图像上，随机的出现n个0-9的数字，可以重复
            步骤：
                第一步：遍历MNIST数据，生成目标检测的原始图像数据
                    note: 先实现固定大小情况下的固定图像的粘贴; 最终图像中只有两个颜色: 黑+白
                    a. 黑色背景图像，大小随机在300~500之间
                    b. 随机选择5-10张图像随机粘贴到黑色背景图像中
                    c. 随机选择的图像进行随机缩放(等比例缩放), 倍数：0.5~2
                    d. 随机选的图像进行随机的旋转操作，旋转角度: -30~30
                    e. 每张数字图像粘贴的时候，间隔至少20个像素
                    f. 每张保存输出的图像数据中，随机一个白色的28~50像素之间的字母
                    g. 产生三份数据:
                        第一份数据满足a、b、c、e条件 + 每张图像中只有最多2个数字
                        第二份数据满足a、b、c、e条件
                        第三份数据满足a、b、c、e、d、f条件
                        note: 100张图像，需要包含0-9的所有数字
                第二步：用labelimg进行数据边框标注
                    分别标注数字0-9&字母
                        类别区分方式：
                            区分方式一(2个类别)：数字、字母
                            区分方式二(11个类别)：数字1、数字2、数字3.....数字9、数字0、字母
    20251102:
        -1 在20251026作业1和作业2的基础上，结合YOLO的目标检测训练代码，完成目标检测模型训练、推理、部署相关代码。